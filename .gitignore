# Build output
/pyrite
/dist/
*.exe

# Local configuration and secrets — never commit API keys
.env
.env.*
config.json
*.local.json

# Runtime data: market cache, AI cache, saved runs
/.pyrite/
/data/cache/
/data/runs/

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp

# Agent worktrees — local scratch, never part of the repository
.claude/worktrees/

# Python bytecode
__pycache__/
*.py[cod]
